# Placeholder credentials for local manual testing only.
# Replace these values (or better, generate the Secret from a vault/sealed-secret
# pipeline) before using this manifest anywhere near production.
apiVersion: v1
kind: Secret
metadata:
  name: ledgerx-secrets
  namespace: ledgerx
  labels:
    app.kubernetes.io/part-of: ledgerx-backend
type: Opaque
stringData:
  DB_USER: "ledgerx"
  DB_PASSWORD: "ledgerx"
  RABBITMQ_USER: "ledgerx"
  RABBITMQ_PASSWORD: "ledgerx"
  POSTGRES_DB: "ledgerx"
  POSTGRES_USER: "ledgerx"
  POSTGRES_PASSWORD: "ledgerx"
  RABBITMQ_DEFAULT_USER: "ledgerx"
  RABBITMQ_DEFAULT_PASS: "ledgerx"
